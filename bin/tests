#!/usr/bin/env bash

ROOT=$PWD
JEKYLL_PROJECT="project"
APP__VERSION="1.0.8"

bin/setup
bundle exec rake build

# mkdir -p "$ROOT/$JEKYLL_PROJECT/.bundle"

# # Add dependency in Gemfile for Jekyll project
# cat << EOF > "${ROOT}/${JEKYLL_PROJECT}/Gemfile"
# source 'https://rubygems.org'

# gem 'sass-embedded', '~> 1.69'
# gem "logger", "~> 1.7.0"
# gem "csv", "~> 3.3.2"
# gem "colorize", "~> 1.1.0"
# gem "rake", "~> 13.3.0"
# gem "sass", "~> 3.7.4"
# gem "erb", "~> 5.0.2"
# gem "bigdecimal", "~> 3.0.2"
# gem "webrick", "~> 1.7.0"
# gem "json", "~> 2.5.1"
# gem 'jekyll', '~> 4.4.1'

# group :jekyll_plugins do
#   gem 'jektify', '~> 1.0.8'
# end
# EOF

# cat << EOF > "$ROOT/$JEKYLL_PROJECT/.bundle/config"
# ---
# BUNDLE_PATH: "vendor/bundle"
# BUNDLE_DISABLE_SHARED_GEMS: "true"
# EOF

# cd "$ROOT/$JEKYLL_PROJECT"

# bundle exec jekyll new .
# cd project
# sed -i "/  gem \"jekyll-feed\", \"~> 0.15.1\"/a \  \gem \"jektify\", \"~> $APP__VERSION\"" Gemfile
# sed -i "/- jekyll-feed/a\ \ - jektify" _config.yml

# cat << EOF >> _config.yml

# jektify:
#   enable: true
#   open: true
#   toggle:
#     enable: true
#   spotify:
#     user: # "you_user_spotify"
#     text: "See me no Spotify"
#   sass:
#     jekyll: true
#     other:
#       dir: "way/to/your/SASS"
#   title:
#     enable: false
#     text: "Music name"
#   description:
#     enable: false
#     text: |
#       Jekyll plugin to generate html snippets for embedding Spotify Musics.
#       To listen to the full song, open your Spotify and start these musics.
# EOF

# echo "{% jektify spotify/track/62qsgMnY4wg8nE5qjyOdWO/dark %}" >> _posts/*-welcome-to-jekyll.markdown

# # Re-Update Gemfile.lock for project Jekyll
# bundle install

# # Compiled project Jekyll with liquid of gem Jektify
# bundle exec jekyll b
# echo "${APP__NAME^^} $JEKYL__PROJECT compiled!"
